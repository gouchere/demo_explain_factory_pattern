export default class Media{
    constructor(data){
        this.title=data.title
        this.price=data.price
        this.name=data.name
    }

    render(){
        // render default card view
    }
}